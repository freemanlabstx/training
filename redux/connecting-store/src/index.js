import React from 'react';
import ReactDOM from 'react-dom';

import ConnectedComponent from './ConnectedComponent';

const root = document.getElementById("root");
if (root) { ReactDOM.render(<ConnectedComponent />, root); }

